<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <TextView
            android:id="@+id/confirmBillTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textAlignment="center"
            android:text="Verify this Bill "
            android:textSize="18dp"
            android:textStyle="bold"
            android:maxLines="2"
            android:layout_margin="32dp"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/okConfirmBillBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="10dp"
                android:layout_weight="1"
                android:text="Confirm"
                app:backgroundTint="@android:color/holo_green_light"
                app:cornerRadius="20dp"
                app:icon="@drawable/ic_baseline_check_32"
                app:iconGravity="top" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/cancelConfirmBillBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="10dp"
                android:layout_weight="1"
                android:text="CANCEL"
                app:backgroundTint="@android:color/holo_red_light"
                app:cornerRadius="20dp"
                app:icon="@drawable/ic_baseline_error_32"
                app:iconGravity="top" />
        </LinearLayout>

    </LinearLayout>
</layout>